.env

# ----------------------------------------------------------------------
# 1. Python 가상 환경 및 캐시 (Virtual Environment & Cache)
# ----------------------------------------------------------------------
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.venv
venv
env

# ----------------------------------------------------------------------
# 2. 빌드 및 배포 관련 파일 (Build & Distribution)
# ----------------------------------------------------------------------
dist/
build/
*.so
wheels/

# ----------------------------------------------------------------------
# 3. 개발 도구 및 IDE 설정 (IDE & Editor Files)
# ----------------------------------------------------------------------
.vscode/  # VS Code 설정 폴더 (필요시 제외 가능)
.idea/    # IntelliJ / PyCharm 설정 폴더
*.swp     # Vim 스왑 파일

# ----------------------------------------------------------------------
# 4. 데이터베이스 및 로그 (Database & Logs)
# ----------------------------------------------------------------------
*.sqlite3 # SQLite 파일 (개발용 DB)
*.db
log.txt
*.log

# ----------------------------------------------------------------------
# 5. Node.js / 프론트엔드 (Node.js & Frontend)
# ----------------------------------------------------------------------
front-end/node_modules/          # npm 패키지 폴더
package-lock.json      # 패키지 잠금 파일 (선택적)
yarn.lock              # yarn 잠금 파일
pnpm-lock.yaml         # pnpm 잠금 파일
.npm/                  # npm 캐시
.yarn/                 # yarn 캐시
front-end/dist/        # Vite 빌드 결과물
.vite/                 # Vite 캐시
*.local                # 로컬 환경 변수